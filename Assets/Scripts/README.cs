using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class README : MonoBehaviour
{


    /*
        General Reminders/Practices
            Stat Order --> 
                    STR (0)         DEX(1) 
                    CON (2)         INT(3) 
                    WIS (4)         CHA(5)
            Variable name exceptions
                Intelligence --> intel (NOT INT)

     */



    /*
        TO DO:
        ~find resolution fix (keep aspect ratio on window resize)
                    currentsolution --> StaticResolution Script (Windows Only 16:9)
     */
}

